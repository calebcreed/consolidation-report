{
  "name": "restaurant",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/restaurant/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p apps/restaurant/tsconfig.app.json",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
